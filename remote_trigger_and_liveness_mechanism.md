# 원격 트리거와 생존 확인 메커니즘

## 1. 문서 목적

이 문서는 사외에서 사내 PC의 스킬 업데이트를 **원하는 시점에 실행**시키고, 그 PC가 **살아 있는지**를 원격에서 확인하는 제어 계약을 정의한다.

관측 문서(`job_list_fail_pass_observation_mechanism.md`)와 역할이 다르다.

| 문서 | 다루는 것 | 질문 |
|---|---|---|
| 관측 메커니즘 | 실행이 **어디까지 진행됐는지** 판정 | "설치가 성공했나?" |
| 이 문서 | 실행을 **언제 시작시킬지**와 **살아 있는지** | "지금 돌릴 수 있나? 애초에 살아 있나?" |

두 계약은 같은 신호 채널(Release Asset `download_count`)을 쓰지만 다른 Asset 집합을 사용한다. 신호 요청 정책, 전파 지연, 오판 방지 규칙은 관측 문서의 것을 그대로 따른다.

이 문서도 특정 버전에 종속되지 않는다.

---

## 2. 해결하는 문제

### 2.1 세 가지 목표

| 목표 | 기존 구조의 한계 |
|---|---|
| 원하는 시점에 업데이트 | 고정 주기 폴링(예: 4시간). 최악의 경우 그만큼 지연되고 즉시 실행 수단이 없다 |
| 원격으로 스킬 실행 | 트리거 수단이 폴링뿐이라 실행 시점을 제어할 수 없다 |
| 정상 동작 확인 | **"할 일이 없어 조용한 것"과 "고장나서 조용한 것"이 구분되지 않는다** |

### 2.2 세 번째가 앞의 둘의 전제다

관측 신호가 전부 `0`일 때, 그것이 정상인지 고장인지 알 수 없으면 나머지 두 목표는 확인 자체가 불가능하다. "업데이트를 걸었는데 반응이 없다"가 트리거 실패인지, 엔진 고장인지, 원래 할 일이 없었던 것인지 판별되지 않기 때문이다.

실제로 이 구분이 없어서, 예약 실행이 끊긴 상태를 몇 시간 동안 확정하지 못한 사례가 있었다. 원인은 다음과 같았다.

> 설치 레이아웃을 바꾸는 마이그레이션이 실행 파일 위치를 옮겼는데, 예약 작업 등록은 옛 경로를 계속 가리켰다. 작업은 매 주기 정상적으로 트리거되어 "실행 시각"까지 갱신했지만, 실행 파일이 없어 프로세스가 시작조차 못 했다. 로그도, 신호도, 실패 보고도 전혀 남지 않았다.

**끊긴 자동화는 실패 신호를 내지 못한다.** 실행 주체가 시작을 못 하기 때문이다. 그래서 이 계약의 첫 번째 요구사항은 **무조건 발사되는 생존 신호**다.

---

## 3. 배치 원칙

생존을 책임지는 구성 요소는 **마이그레이션 대상 트리 바깥**에 둔다.

```text
~/l1sw-dispatcher/          <- 여기. 스킬 트리와 분리
~/l1sw-private-skills/      <- 스킬 canonical root (마이그레이션 대상)
~/.claude/skills/           <- discovery entry (마이그레이션 대상)
```

이유는 2.2의 사고가 정확히 이것이기 때문이다. 스킬 트리 안에 두면 레이아웃 변경 때마다 등록이 끊기고, 끊긴 뒤에는 스스로 복구할 수단도 없다. 생존 확인 장치가 감시 대상과 운명을 같이하면 안 된다.

같은 이유로 이 구성 요소는 다음을 만족해야 한다.

- 표준 라이브러리만 사용한다. 설치된 스킬이나 실행 게이트웨이에 의존하지 않는다.
- 자주 바뀌지 않는다. 변하는 것은 엔진과 스킬이고, 이 계층은 안정적으로 유지한다.
- 단일 파일로 동작한다. 복사만으로 배치할 수 있어야 한다.

---

## 4. 구성 요소

```text
[사외/유지보수 PC]                    [GitHub]                 [사내 PC]

 trigger 문서 발행  ─────────────>  automation/trigger.json
                                                          <──── 주기 폴링
                                    Release Asset          ────> 신호 GET
 count 관측       <─────────────    download_count
```

| 요소 | 역할 |
|---|---|
| trigger 문서 | 실행 요청을 담는 작은 JSON. `nonce`가 바뀌면 "실행하라"는 뜻 |
| dispatcher | 사내 PC에 상주. 주기적으로 trigger를 확인하고 필요할 때만 엔진 호출 |
| 생존 신호 | dispatcher가 매 사이클 무조건 발사 |
| 엔진 | 실제 업데이트/작업 수행 주체. dispatcher가 호출한다 |

**핵심 분리**: "확인"은 자주·가볍게, "실행"은 드물게·무겁게. dispatcher는 매 주기 작은 JSON 하나만 받고, 실제 엔진은 요청이 있을 때만 부른다.

---

## 5. 사이클 프로시저

dispatcher가 한 번 깨어날 때 수행하는 순서다. 각 단계의 **순서 자체가 계약**이며, 임의로 바꾸면 보장이 깨진다.

```text
[스케줄러 발화]
     │
 (1) 생존 신호 발사 ──────────── 무조건. 가장 먼저.
     │
 (2) 중복 실행 잠금
     ├─ 이미 실행 중 ──────────> 종료
     └─ 획득
     │
 (3) 자기 등록 점검
     ├─ 미등록 ────────────────> 기록만 하고 통과
     ├─ 정상 ──────────────────> 통과
     └─ 경로 깨짐 ─────────────> 재등록 + 자기치유 신호
     │
 (4) trigger 문서 수신
     ├─ 실패 ──────────────────> 상태 기록 후 종료
     └─ 성공
     │
 (5) 주기 변경 요청 확인
     ├─ 없음 / 동일 ───────────> 통과
     └─ 다름 ──────────> 범위 제한 → 재등록 + 주기변경 신호
     │
 (6) nonce 비교
     ├─ 이전과 동일 ───────────> 상태 기록 후 종료 (엔진 호출 안 함)
     └─ 달라짐
     │
 (7) 트리거 신호 발사 + nonce를 먼저 저장
     │
 (8) 엔진 실행
     │
 (9) 결과 기록 + 성공/실패 신호
     │
 (10) 잠금 해제
```

### 5.1 각 단계가 그 자리에 있는 이유

**(1) 생존 신호가 맨 앞인 이유.** 이 뒤의 모든 단계는 실패할 수 있다. 네트워크가 끊기고, 파일이 깨지고, 엔진이 죽을 수 있다. 그 어떤 경우에도 "스케줄러가 나를 깨웠다"는 사실만은 관측되어야 한다. 뒤로 옮기면 앞 단계 실패가 생존 신호까지 삼켜버려, 고장과 침묵이 다시 구분되지 않는다.

**(2) 잠금이 두 번째인 이유.** 엔진 실행은 수 분이 걸릴 수 있다. 주기가 짧으면 이전 사이클이 끝나기 전에 다음이 깨어난다. 잠금이 없으면 엔진이 중첩 실행된다. 다만 잠금 실패로 종료하더라도 (1)은 이미 발사됐으므로 생존은 여전히 관측된다.

잠금은 **경과 시간 기준으로 만료**시킨다. 프로세스가 비정상 종료해 잠금 파일이 남으면 영구히 막히기 때문이다. 만료 시간은 엔진 최대 실행 시간보다 넉넉하게 잡는다.

**(3) 자기 등록 점검이 실행보다 앞선 이유.** 등록이 깨진 채로 엔진만 계속 돌면, 이번 사이클은 성공해도 다음 사이클이 오지 않는다. 실행보다 지속성을 먼저 확보한다.

**(6) nonce 비교로 엔진 호출을 막는 이유.** 이것이 "가볍게 자주 확인, 무겁게 드물게 실행"의 실현 지점이다. 주기가 짧아도 실제 부하는 작은 JSON 하나를 받는 것뿐이다.

**(7) nonce를 엔진 실행 *전에* 저장하는 이유.** 순서를 바꾸면 무한 재시도가 발생한다. 엔진이 실행 중 죽으면 nonce가 저장되지 않고, 다음 사이클이 같은 nonce를 "새 것"으로 보아 다시 실행하며, 이것이 영원히 반복된다. 먼저 저장하면 실패한 요청은 한 번만 시도되고, 재시도가 필요하면 발행자가 nonce를 새로 발급한다.

이 선택의 대가는 "엔진이 죽었을 때 자동 재시도가 없다"는 것이다. 무한 루프보다 이쪽이 안전하다고 판단한 결과이며, 실패는 (9)의 신호로 관측되므로 발행자가 알고 대응할 수 있다.

---

## 6. trigger 문서 계약

```json
{
  "nonce": "<매 발행마다 달라지는 값>",
  "issued_at": "<발행 시각>",
  "note": "<사람이 읽을 메모, 선택>",
  "interval_min": "<주기 변경 요청, 선택>"
}
```

규칙:

- **`nonce`만이 실행 여부를 결정한다.** 다른 필드가 바뀌어도 nonce가 같으면 실행하지 않는다.
- nonce는 **불투명한 값**이다. dispatcher는 내용을 해석하지 않고 이전 값과 같은지만 본다.
- 같은 작업을 다시 실행하려면 nonce를 **새로 발급**한다. 기존 값을 재사용하지 않는다.
- `note`는 관측되지 않는다. 사후에 로컬 로그에서 무엇을 의도했는지 확인하는 용도다.

문서는 작게 유지한다. 매 주기 수신되므로 크기가 곧 상시 비용이다.

---

## 7. 주기 제어

주기는 양쪽에서 조절할 수 있다.

| 방법 | 절차 | 반영 시점 |
|---|---|---|
| 대상 PC에서 | 설치 명령에 주기를 지정해 재등록 | 즉시 |
| 원격에서 | trigger 문서에 `interval_min` 포함 | 다음 사이클 |

원격 조절은 **반드시 범위 제한을 거친다.** 요청값이 허용 범위를 벗어나면 가장 가까운 경계값으로 고정한다.

범위 제한이 필요한 이유는 두 가지다. 하나는 실수로 지나치게 짧은 값을 넣었을 때 대상 PC가 과부하되는 것을 막기 위함이고, 다른 하나는 지나치게 긴 값이 생존 확인 주기를 사실상 무력화하는 것을 막기 위함이다.

주기가 실제로 바뀌면 별도 신호를 발사한다. 원격에서 요청이 반영됐는지 확인할 수단이 그것뿐이기 때문이다.

---

## 8. 자기 치유와 그 한계

dispatcher는 매 사이클 자기 등록을 점검하고, 등록이 존재하지만 **참조 경로가 유효하지 않으면** 재등록한다.

### 8.1 등록이 아예 없을 때는 만들지 않는다

의도적인 설계다. 수동으로 한 번 실행했다고 해서 예약이 조용히 생기면 예측 불가능한 동작이 된다. 등록 생성은 명시적인 설치 명령의 몫이고, 미등록 상태는 상태 조회에서 분명히 보고한다.

### 8.2 순환 한계를 인정한다

**자기 치유는 dispatcher가 실행될 수 있을 때만 동작한다.** 등록이 완전히 깨져 프로세스가 시작조차 못 하면 스스로 고칠 수 없다.

따라서 자기 치유는 최후의 방어선이 아니라 **부차적 안전망**이다. 진짜 방어는 §3의 배치 원칙이다. 마이그레이션이 건드리지 않는 위치에 두어 애초에 등록이 깨지지 않게 하는 것.

자기 치유가 실제로 값을 하는 경우는 다음과 같다.

- 스크립트가 다른 위치로 복사되어 등록과 실제 위치가 어긋났을 때
- 주기 설정이 의도와 달라졌을 때
- 사람이 수동으로 한 번 실행해 주는 것만으로 등록이 복구될 때

---

## 9. 신뢰성 속성

| 속성 | 구현 방식 | 없으면 생기는 문제 |
|---|---|---|
| 생존 신호 우선 | 사이클 최초 동작 | 앞 단계 실패가 생존 관측을 삼킨다 |
| 예외 격리 | 모든 단계가 예외를 흡수하고 사이클은 항상 정상 종료 | 한 단계 실패가 전체를 중단시킨다 |
| 중복 실행 방지 | 만료 시간이 있는 잠금 | 엔진 중첩 실행 |
| 무한 재시도 방지 | 엔진 실행 전 nonce 저장 | 실패한 요청이 영구 반복 |
| 원자적 상태 기록 | 임시 파일 기록 후 교체 | 중단 시 상태 파일 손상 |
| 로그 보존 | 크기 기준 회전 | 디스크 무한 증가 |
| 콘솔 비의존 | 표준 출력이 없어도 동작 | 예약 실행 경로에서만 죽는다 (§13.1) |
| 창 억제 | 모든 자식 프로세스에 창 생성 억제를 명시 | 주기마다 창이 튀어나온다 (§13.2) |
| 인코딩 무관 출력 처리 | 바이트로 받아 명시적 디코딩 | 도구별 인코딩 차이로 출력이 유실된다 (§13.3) |

### 9.1 예외 격리의 의미

"never raises"는 **함수 정의 내부**에서 보장되어야 한다. 호출부의 예외 처리에만 의존하면, 그 함수가 다른 경로로 재사용되거나 내부 로직이 늘어날 때 보장이 조용히 깨진다. 관측 문서 §10.1과 같은 원칙이다.

---

## 10. 신호 계약

관측 문서 §4.4의 이름 규칙(버전 비종속, `<주체>-<단계>.signal`)을 그대로 따른다.

```text
dispatcher-heartbeat.signal          매 사이클 무조건
dispatcher-trigger-fired.signal      nonce 변경을 감지해 실행을 시작함
dispatcher-engine-ok.signal          엔진이 정상 종료
dispatcher-engine-fail.signal        엔진이 비정상 종료
dispatcher-selfheal.signal           깨진 등록을 재등록함
dispatcher-interval-changed.signal   주기 변경 요청을 반영함
```

### 10.1 판정표

**이 표가 이 문서의 존재 이유다.** 생존 신호 하나로 기존의 근본적 모호성이 해소된다.

| heartbeat | 다른 신호 | 판정 |
|---|---|---|
| 증가 | 전부 `0` | **정상.** 살아 있고 할 일이 없었음 |
| **`0`** | 전부 `0` | **고장.** 스케줄러 또는 dispatcher 미실행 |
| 증가 | `trigger-fired` `0` | 살아 있으나 요청을 못 봄. trigger 문서 도달 실패 또는 nonce 미변경 |
| 증가 | `trigger-fired` 증가, `engine-*` `0` | 엔진 호출 도중 중단 |
| 증가 | `engine-fail` 증가 | 엔진이 실행됐고 실패함. 원인은 로컬 증거로 확정 |
| 증가 | `selfheal` 증가 | 등록이 깨져 있었고 복구됨. **원인 조사 대상** |

`selfheal`이 반복적으로 증가하면 무언가가 계속 등록을 깨고 있다는 뜻이므로, 복구에 만족하지 말고 원인을 찾는다.

### 10.2 전파 지연

관측 문서 §13.1과 동일하게 적용된다. 신호 발사와 count 반영 사이에 지연이 있으므로, 판정 시각은 사이클 종료 예상 시각에 그 여유를 더한 뒤로 잡는다.

### 10.3 생존 신호는 횟수가 아니라 유무로 읽는다

**heartbeat의 delta를 사이클 수로 해석하면 안 된다.**

관측 중 실행 횟수보다 집계값이 작은 구간이 반복적으로 나타났다. 자주 발사되는 Asset일수록 이 격차가 컸다. 격차의 원인은 확정되지 않았다. 관측된 사실은 다음과 같다.

- 시간이 지나면 집계값이 계속 올라간다. 즉 상당 부분은 **전파 지연**이다.
- 자주 발사되는 Asset(heartbeat)이 드물게 발사되는 Asset(`trigger-fired`, `engine-ok`)보다 뒤처지는 경향이 있었다.
- 어느 시점의 스냅샷도 그 순간까지의 실행 횟수와 일치하지 않았다.

지연과 그 밖의 요인을 분리하려면 **발사를 완전히 멈춘 조용한 구간**을 두고 충분히 기다린 뒤 최종값을 읽어야 한다. 그 확인 전까지 다음 규칙을 적용한다.

- **올바른 해석**: "이 구간에 최소 한 번은 살아 있었다"
- **잘못된 해석**: "이 구간에 정확히 N번 깨어났다"

이 규칙은 원인이 무엇으로 밝혀지든 유효하다. 생존 판정은 증가 여부만 사용하므로 영향을 받지 않는다.

정확한 사이클 수가 필요하면 대상 PC의 로컬 기록을 사용한다. 원격 count는 그 용도로 설계되지 않았다.

**주의**: 스냅샷 하나로 "신호가 유실됐다"고 판단하지 않는다. 값이 아직 오르는 중일 수 있으며, 실제로 그런 사례가 있었다. 시간 간격을 두고 두 번 읽어 값이 고정됐는지 먼저 확인한다.

### 10.4 주기가 지연보다 짧을 때

여러 사이클의 신호가 섞여 보인다. 개별 사이클을 분리하려 하지 말고 구간 단위로 해석하며, 10.3에 따라 횟수가 아닌 유무만 판정에 사용한다.

---

## 11. 운영 절차

### 11.1 대상 PC에 배치

```text
1. 스크립트 파일 하나를 안정된 위치에 둔다 (§3)
2. 설치 명령으로 예약 등록 (주기 지정 가능)
3. 상태 조회로 등록이 "정상"인지 확인
4. 스케줄러를 통한 즉시 실행으로 등록된 명령줄을 검증 (§12.2)
```

### 11.2 사외에서 실행 요청

```text
1. trigger 문서를 새 nonce로 생성
2. 저장소에 게시
3. 최대 한 주기 대기
4. trigger-fired 및 engine-* 신호로 결과 확인
```

### 11.3 주기 변경

```text
대상 PC에서: 설치 명령에 새 주기를 지정해 재실행
원격에서:   trigger 문서에 interval_min 포함 후 게시
            (nonce도 함께 바뀌면 실행까지 같이 일어난다)
```

### 11.4 상태 확인

상태 조회는 최소한 다음을 보여야 한다.

```text
스크립트 실제 위치
현재 주기와 허용 범위
예약 작업 등록 여부, 상태, 마지막 실행 시각과 결과 코드
등록 건전성 (참조 경로가 실제로 존재하는지)
누적 사이클 수
마지막 trigger 판정 결과와 시각
마지막으로 본 nonce
마지막 엔진 종료 코드와 시각
로그 파일 위치
```

---

## 12. 배포 전 검증 게이트

관측 문서 §17과 별개로, 이 구성 요소에는 다음이 추가된다.

1. 신호 Asset 전체가 존재하고 baseline을 기록했다.
2. 수동 실행으로 한 사이클이 정상 종료한다.
3. 같은 nonce로 재실행했을 때 엔진을 호출하지 않는다 (멱등성).
4. **예약 등록 후 스케줄러를 통해 실행했을 때 결과 코드가 `0`이다.**
5. 등록을 고의로 손상시킨 뒤 실행했을 때 자기 치유가 동작한다.
6. 타이머 자연 발화로도 사이클이 수행된다.
7. 전파 지연 후 각 신호 count가 실제 호출 횟수와 일치한다.

### 12.1 3번을 반드시 확인하는 이유

멱등성이 깨지면 **매 주기 엔진이 재실행된다.** 주기가 짧을수록 피해가 크고, 겉으로는 "잘 도는 것"처럼 보여 발견이 늦는다.

### 12.2 4번이 핵심이다

**수동 실행 성공은 예약 실행 성공을 보장하지 않는다.** 두 경로는 실행 주체, 인터프리터, 작업 디렉터리, 콘솔 유무, 권한 컨텍스트가 모두 다르다.

§2.2의 사고가 정확히 이 차이에서 왔다. 수동으로는 잘 되는데 예약에서만 실패했고, 그 실패는 아무 흔적도 남기지 않았다. 스케줄러를 통한 강제 실행은 타이머를 기다리지 않고 이 경로를 검증하는 수단이며, 등록된 명령줄이 실제로 동작하는지를 확인한다.

6번(타이머 자연 발화)은 4번과 다른 것을 본다. 4번은 명령줄의 정확성을, 6번은 타이머 설정 자체를 검증한다.

---

## 13. 발견된 실패 사례

네 건 모두 **예약 실행 경로에서만 나타나거나 그 경로에서 훨씬 심각해지는** 결함이었다. 수동 실행 검증만 했다면 대부분 배포 후에야 드러났을 것이다.

| # | 결함 | 수동 실행에서 관측되는가 |
|---|---|---|
| 13.1 | 표준 출력 부재로 사이클 전체 무력화 | 안 됨 |
| 13.2 | 자식 프로세스가 창 생성 | 안 됨 |
| 13.3 | 로케일 디코딩 실패로 출력 유실 | 안 됨 |
| 13.4 | 진단 기능이 거짓 보고 | 되지만 놓치기 쉬움 |

이것이 §12.2를 게이트 항목으로 유지하는 이유다.

### 13.1 콘솔 없는 실행에서의 표준 출력

예약 작업은 창 없이 실행되는 인터프리터를 사용하는 것이 일반적이다. 그 환경에서는 표준 출력이 존재하지 않으며, 출력 함수 호출이 예외를 발생시킨다.

로그 기록 함수가 화면 출력과 파일 기록을 함께 수행하고 있었기 때문에, 이 예외가 로그 기록 전체를 무력화했다. 사이클의 모든 단계가 로그를 호출하므로 결과적으로 아무것도 동작하지 않았을 것이다.

**교훈**: 예약 실행되는 코드는 표준 출력의 존재를 가정하지 않는다. 화면 출력은 선택적 부가 기능으로 취급하고, 내구성 있는 기록은 파일에 남긴다.

### 13.2 콘솔 없는 부모가 만든 자식 창

예약 실행되는 프로세스에는 콘솔이 없다. 그 상태에서 콘솔 응용프로그램을 자식으로 실행하면 운영체제가 **자식에게 새 창을 만들어 준다.**

이 구성 요소는 매 사이클 셸 유틸리티를 호출해 등록 상태를 점검하고, 요청이 있을 때 셸을 통해 엔진을 실행한다. 창 억제를 명시하지 않으면 **매 주기 창이 튀어나온다.** 기능은 정상 동작하므로 로그만 보아서는 알 수 없고, 실제 예약 운영 중에 사용자가 목격해야 발견된다.

**교훈**: 예약 실행되는 코드에서 자식 프로세스를 만들 때는 창 억제를 명시한다. 부모에게 콘솔이 없다는 사실이 자식에게 자동으로 상속되지 않는다.

### 13.3 로케일 디코딩으로 인한 출력 유실

자식 프로세스 출력을 문자열로 자동 변환하도록 맡기면 **로케일 인코딩**으로 디코딩된다. 그런데 이 구성 요소가 호출하는 도구들은 인코딩이 서로 다르다. 업데이트 엔진은 UTF-8로 출력하고, 운영체제 콘솔 유틸리티는 시스템 코드 페이지로 출력한다.

한쪽 기준으로 디코딩하면 다른 쪽에서 디코딩 예외가 발생하고, 출력을 읽는 내부 스레드가 종료되면서 **해당 실행의 출력 전체가 유실된다.** 종료 코드는 정상적으로 반환되므로 "성공했지만 아무 내용도 남지 않은" 기록이 된다.

원격 신호는 성공/실패만 알려줄 뿐 원인을 담지 못한다. 그 원인을 확정할 유일한 수단이 로컬 기록인데, 그 기록이 비어 버리는 것이므로 손실이 크다.

**교훈**: 여러 도구의 출력을 다루는 지점에서는 자동 문자열 변환에 의존하지 않는다. 바이트로 받아 명시적으로 디코딩하고, 어떤 입력에도 예외가 나지 않도록 대체 문자 처리를 적용한다.

### 13.4 진단 기능이 거짓을 보고한 사례

상태 조회가 예약 작업 정보를 읽기 위해 셸 명령을 문자열로 조립하고 있었는데, 문자열 조립 방식이 섞이면서 중괄호 이스케이프가 일부 구간에서만 적용됐다. 그 결과 셸에 문법이 깨진 명령이 전달됐고, 조회는 항상 빈 결과를 반환했다.

**등록이 정상인데도 상태 조회는 "등록되지 않음"이라고 답했다.**

이 유형은 단순한 기능 결함보다 위험하다. 진단 도구가 거짓을 보고하면 운영자는 멀쩡한 것을 고치려 하고, 실제 문제는 계속 방치된다.

**교훈**: 진단 기능은 그 자체가 검증 대상이다. "도구가 문제없다고 말한다"를 근거로 삼기 전에, 도구가 알려진 상태를 올바르게 보고하는지 먼저 확인한다. 이 사례에서는 등록이 존재한다는 사실을 다른 수단으로 이미 알고 있었기 때문에 불일치가 드러났다.

---

## 14. 한계와 비적용 범위

- **완전한 즉시 실행은 아니다.** 실제 지연은 두 구간의 합이다.

  ```text
  총 지연 = trigger 문서 게시 반영 지연 + 최대 한 주기
  ```

  게시 반영 지연은 저장소 원본 파일을 캐시 경유로 받기 때문에 발생하며, 실측에서 약 2~3분이 걸렸다. 캐시 무효화 헤더를 보내도 즉시 반영되지는 않는다. 이 값은 주기와 무관한 고정 비용이므로, 주기를 아무리 줄여도 그 아래로 내려가지 않는다.

  더 짧은 반영이 필요하면 캐시를 거치지 않는 경로를 쓸 수 있으나, 그 경로는 요청 한도가 있으므로 주기와 함께 검토해야 한다. 수십 분 단위 주기에서는 이 지연이 전체의 작은 부분이므로 캐시 경로로 충분하다.
- **엔진 실패를 자동 재시도하지 않는다.** §5.1의 무한 루프 방지 선택의 대가다. 재시도는 새 nonce 발급으로 한다.
- **대상 PC에서 외부로 데이터를 보내지 않는다.** 관측은 어느 주소를 요청했는가만으로 이루어지며, 실행 결과 본문은 전송되지 않는다. 상세 원인은 대상 PC의 로컬 기록으로만 확정된다.
- **첫 배치는 원격으로 할 수 없다.** 최초 설치와 예약 등록은 대상 PC에서 직접 수행해야 한다. 이 계층 자체를 원격으로 부트스트랩할 수단은 없다.

---

## 15. 운영 요약

```text
[사외]  trigger 문서를 새 nonce로 게시
          │
[사내]  다음 주기에 dispatcher 깨어남
          ├ 생존 신호 발사 (항상)
          ├ 등록 점검 및 필요 시 자기 치유
          ├ trigger 수신 및 주기 반영
          └ nonce 변경 감지 → 엔진 실행
          │
[사외]  전파 지연 후 count 관측
          ├ heartbeat 증가 여부로 생존 판정
          ├ trigger-fired로 요청 도달 판정
          └ engine-ok / engine-fail로 결과 판정
          │
        이상 시 대상 PC의 로컬 기록으로 원인 확정
```

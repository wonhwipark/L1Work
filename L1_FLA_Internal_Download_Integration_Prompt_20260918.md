# L1-FLA 사내 로그 수집 기능 도입 — 분석·구현 프롬프트

작성일: 2026-09-18

## 사용 방법

사내에서 `l1sw-dev-analyzer`와 `l1-fla` 파일을 읽을 수 있는 LLM 작업창에 이 문서를 첨부하고 다음과 같이 요청하세요.

> 이 문서의 실행 지시문에 따라 사내 두 스킬을 직접 분석하고, 적합한 연결 방식을 선택해 L1-FLA 수정과 검증까지 진행해줘. 경로는 먼저 자동 탐색하고, 실제로 찾을 수 없는 정보만 질문해줘.

L1-FLA가 설치되어 있지 않으면 `l1-fla_v0_3_28_0914.zip`을 함께 제공하세요. 이 문서는 구현 요청이며, 수정된 스킬 패키지는 아닙니다. 사내 원본을 외부에 제공할 필요가 없습니다.

---

## 실행 지시문

### 1. 목표와 완료 범위

사내 `l1sw-dev-analyzer`는 Jira에서 로그를 인식하고 여러 형태로 다운로드하는 능력이 우수하지만, 여러 Jira 이슈를 일괄 처리하는 기능이 부족하다. 실제 구현을 확인하여 이 장점을 `l1-fla`에서 재사용하도록 수정하라.

이번 작업은 **실제 파일 분석 → 연결 방식 결정 → FLA 구현 → 검증 → 변경 결과 제공**까지 수행한다. 분석 보고서나 TODO만 작성하고 종료하지 말고, 환경에서 수행 가능한 단계는 이어서 완료하라. 원본에 없는 명령·API·지원 기능은 만들었다고 가정하지 말라.

| 구성요소 | 맡을 역할 |
|---|---|
| L1-FLA | 복수 Jira 순차 처리, 제외 규칙, 분석 포인트 정리, 진행·재개, 결과 집계 |
| 사내 스킬에서 재사용할 수집 기능 | 로그 위치 인식, 이슈와 관련된 로그 선택, 포털·주소 해석, 인증 연계, 실제 파일 다운로드 |
| FLA에 설정된 Analyzer | 수집 완료된 로컬 로그 분석. 기존 기본 `issue-analyzer`와 사용자 설정 유지 |

핵심은 **로그 탐색·선택부터 파일 확보까지** 재사용하는 것이다. 이미 알려진 URL을 다운로드하는 함수만 연결해서 Jira 로그 인식 개선까지 완료했다고 판단하지 말라.

### 2. 실제 설치 위치와 구현 확인

사용자가 지정한 경로가 있으면 우선 사용한다. 없으면 작업공간과 통상적인 `~/l1sw-skills/`, `~/l1sw-private-skills/` 아래에서 두 스킬의 `SKILL.md`, 실제 설치 대상, 실행 진입점을 찾는다. Windows에서는 실제 사용자 경로와 Git Bash 경로를 확인한다. 전체 디스크를 무차별 탐색하지 않는다.

적용되는 `AGENTS.md`와 작업 지침을 읽고, 두 스킬의 버전·경로·Git 상태·기존 사용자 변경을 기록한다. 이 문서가 작성될 때 확인한 FLA는 v0.3.28이지만, 사내에 더 최신 버전이 있으면 그 버전을 기준으로 진행하며 다운그레이드하지 않는다.

`l1sw-dev-analyzer`는 우선 읽기 전용으로 조사한다. 문서뿐 아니라 실제 호출되는 스크립트, 참조 문서, 필요한 하위 스킬·MCP 연계까지 추적한다. 관련 없는 전체 분석 흐름을 실행하며 조사하지 않는다.

각 발견은 **확인된 동작 / 문서 설명만 존재 / 미확인**으로 구분하고 파일·함수·설정 등 근거를 남긴다.

| 조사 항목 | 확인할 내용 |
|---|---|
| Jira 취득 | 본문·댓글 전체와 페이지네이션·첨부·Remote Link·구조화 링크를 어디서 어떻게 읽는가 |
| 로그 인식·선택 | 최신 댓글의 재수집 안내, 이전 로그의 대체 관계, 여러 재현 로그, 분석 요청 시점·조건을 어떻게 구분하는가 |
| 주소 해석 | 직접 파일 URL, 포털 목록, 중간 링크, 리다이렉트, 폴더·압축 묶음 등을 어떻게 처리하는가 |
| 지원 경로 | FTP/FTPS/SFTP/HTTP(S)/Cafe/WIZNET/공유 경로 중 실제 지원하는 것과 필요한 의존성은 무엇인가 |
| 인증 | 기존 자격증명·토큰·세션·MCP 연결을 어떻게 재사용하며, 만료·권한 오류는 어떻게 반환하는가 |
| 파일 완결성 | 부분 다운로드, 재시도, 압축 해제, 동명 파일, 재사용 파일, 원본 로그명 보존은 어떻게 처리하는가 |
| 단독 호출 | 전체 이슈 분석을 실행하지 않고 탐색 또는 다운로드만 호출할 수 있는가 |
| 구현 형태 | Python 등 실행 코드인지, LLM 지시문과 MCP 조합인지, 두 방식의 혼합인지 |
| 반환·상태 | 입력, 출력, 종료 코드, 오류, 실제 로컬 경로를 기계적으로 확인할 수 있는가 |
| 운영 의존성 | Windows/Linux 지원, headless 실행 가능 여부, 공유 상태·고정 임시 경로·대화형 질문의 존재 |

나열된 다운로드 형태를 모두 지원한다고 추정하지 말라. 사내 원본에서 발견한 추가 방식은 조사에 포함한다. 소스가 없거나 접근할 수 없으면 관련 구현을 완료했다고 하지 말고, FLA 쪽 비교 결과를 정리한 뒤 필요한 경로·자료만 질문한다.

### 3. 연결 방식 결정

다음 순서로 검토하되 실제 코드 근거와 유지보수 비용으로 결정한다.

| 선택 | 적용 조건 | 구현 방향 |
|---|---|---|
| A. 기존 수집 기능 호출 — 우선 검토 | 탐색·수집만 실행할 수 있고 입출력·실패 처리가 명확함 | FLA에 얇은 연결 모듈을 두고 공개 진입점 또는 안정된 모듈을 호출 |
| B. 필요한 기능 이식 | 전체 분석과 강하게 묶였거나 호출 계약이 불안정함 | 필요한 탐색·해석·수집 코드와 최소 의존성을 FLA의 별도 모듈로 분리 |
| C. 부분 결합 | 탐색 규칙과 다운로드 실행의 재사용 가능성이 다름 | 탐색 규칙은 FLA에 도입하고, 안정된 다운로드 기능만 호출하는 등 필요한 조합 선택 |

사내 스킬이 지시문 중심이면 실제 MCP 도구명과 입력·출력, 판단 규칙을 근거로 FLA의 기존 실행 체계에 연결한다. 존재하지 않는 다운로드 CLI나 Python 함수를 가정하지 않는다. 내부 구현에 에이전트가 여러 개 있다는 이유로 그 전체 구성을 이식하지 않는다.

스킬 이름을 Analyzer Registry에 등록하는 것만으로 수집 기능이 연결된다고 판단하지 않는다. 전체 사내 분석기를 이슈마다 실행한 뒤 기존 Analyzer를 다시 실행하는 중복 분석도 만들지 않는다.

선택 이유와 남는 의존성을 짧게 기록하고, 통상적인 구현 선택은 사용자에게 반복 확인하지 말고 진행한다.

### 4. 확인된 FLA 연결 지점

아래는 제공된 **v0.3.28 소스에서 확인한 사실**이다. 현재 사내 버전의 코드와 다시 대조하고 위치가 바뀌었다면 실제 구현을 따른다.

| 파일·함수 | 확인된 역할과 적용 시 주의점 |
|---|---|
| `scripts/l1-fla.py`의 `_command_run_inner()` | Jira key 목록을 순차 처리하고 이슈별 결과를 저장. `--resume`에서 완료 이슈를 재사용 |
| `process_issue()` | Jira 조회, 제외 규칙, Remote Link 조회, 분석 포인트, 탐색·선택, 수집, 분석 전달을 조정 |
| `fetch_jira()`, `fetch_jira_remote_links()` | Jira 취득. 기본 provider는 `mango_mcp`; 기존 명시 설정을 존중 |
| `jira_source_blocks()`, `detect_log_sources()`, `detect_artifact_sources()` | 댓글·본문·구조화 힌트와 저장소 정보를 이용한 후보 탐색 |
| `expand_wiznet_candidates()`, `select_log_candidates()` | 포털 목록 확장과 후보 선택 |
| `acquire_source()`, `run_custom_fetch()` | 실제 파일 수집과 외부 다운로드 명령 연결 |
| `remember-download-method` | 이미 식별된 source를 처리할 외부 명령 등록. Jira 탐색까지 대체하는 범용 인터페이스는 아님 |
| `choose_analysis_units()`, `run_analysis_units()` | 수집된 파일을 분석 단위로 구성하고 설정된 Analyzer 실행 |
| `analysis_handoff.json` | 확정된 로컬 입력 경로와 분석 포인트의 전달 기록 |

v0.3.28의 `run_custom_fetch()`는 실행 전후 새로 생긴 파일을 탐지한다. 기존 파일을 재사용하는 사내 수집기까지 연결할 경우 이 방식만으로는 결과가 누락될 수 있으므로, 수집기 반환 경로와 실제 파일을 검증해 통합하라.

`analysis.analyzers`는 분석기 선택 설정이다. 수집 방식 설정과 분리한다. 설정·스키마·CLI·자연어 라우팅·설명 문서는 실제 제공 기능과 함께 갱신한다.

### 5. 구현 요구사항

**처리 순서와 기존 동작**

Jira 조회 후 기존 제목·상태 제외 규칙을 먼저 적용한다. 제외된 이슈는 새 수집기를 실행하지 않는다. 기존 분석 포인트를 유지하면서 사내 탐색 결과를 연결하고, 실제 로컬 파일 확보 후에만 Analyzer에 전달한다.

FLA가 취득한 Jira 데이터는 가능한 한 전달해 재조회 비용을 줄인다. 사내 기능이 별도 필드나 조회를 요구하면 필요한 범위와 이유를 기록한다. 최초에는 기존 순차 처리를 유지한다.

**수집기의 입출력**

기존 계약이 충분하면 그대로 감싸고, 부족하면 FLA 내부 계약을 정의한다. 다음 정보가 보존되어야 하며, 이 문서의 항목을 사내 스킬에 이미 존재하는 API로 간주하지 않는다.

| 구분 | 필요한 정보 |
|---|---|
| 입력 | Jira key, 취득한 Jira 자료, 분석 포인트, 사용자 지정 source, 목표 저장 위치, timeout·용량 제한, 실행 식별자 |
| 탐색 결과 | 후보 식별자, 출처, 선택·제외 이유, 실제 수집에 필요한 주소 또는 불투명 참조값 |
| 파일 결과 | 실제 로컬 경로, 원본명, 파일 역할, 획득/재사용 여부, 원본 source와의 대응 |
| 상태·오류 | 성공·부분 성공·실패·추가 입력 필요 상태, 단계, 오류 유형, 재시도 가능 여부 |

탐색과 다운로드가 원래 한 함수에 묶여 있으면 억지로 두 번 호출하지 않는다. 대신 후보 선택 근거와 파일 결과를 함께 반환하고 FLA 정책이 적용되도록 한다. 비밀이 포함된 실제 접근값은 실행 내부에서만 사용하고 보고서에는 제거된 값이나 참조 식별자를 남긴다.

**판정·오류 처리**

- 종료 코드 0만으로 다운로드 성공을 판정하지 않는다. 반환된 파일의 존재와 해당 이슈와의 연결을 확인한다.
- HTTP 200으로 로그인 페이지나 오류 페이지가 저장된 경우를 정상 로그로 넘기지 않는다.
- 빈 결과, 인증 만료, 권한 부족, source 미발견, timeout, 일부 파일 실패를 구분한다.
- 내부 수집기가 실패해도 해당 이슈의 실패·부분 결과를 남기고 다음 이슈로 진행한다. 실행 전체 예산 초과 등 명시적인 전체 중단 조건은 유지한다.
- 다른 수집 방식으로 전환할 때 원래 실패 원인과 전환 결과를 보존한다. 같은 조건의 인증 실패를 무한 재시도하지 않는다.
- 수집기 상태를 기존 FLA 상태로 명시적으로 변환한다. 새 상태 문자열 때문에 최종 보고서에서 실패 이슈가 빠지지 않게 한다.

**파일·재개**

- 기존 다운로드 경로 우선순위와 날짜/Jira 레이아웃을 유지하고, 임시 작업은 실행·이슈별로 분리한다.
- 원본 로그명을 보존하고 동명 파일 충돌, 중복 링크·미러, 부분 파일을 처리한다. 대용량 파일의 해시는 기존 결과를 재사용하고 불필요하게 반복 계산하지 않는다.
- 새로 받은 파일과 검증 후 재사용한 파일을 모두 전달한다. 폴더에 있다는 이유만으로 다른 이슈의 파일을 집어넣지 않는다.
- 압축 해제와 용량·파일 수·시간 제한을 기존 FLA 정책과 일치시킨다. 검증된 사용자 입력 경로와 관리하는 임시 경로를 구분한다.
- 중단 후 재개할 때 완료 이슈와 유효한 수집 결과를 재사용한다. 무결성이 확인되지 않은 부분 다운로드는 정상 캐시로 취급하지 않는다.
- SDM과 연관 바이너리·QuickBuild 정보가 발견되면 파일 역할과 대응을 보존한다. 이 정보가 없는데 확보했다고 주장하지 않는다. 이번 작업에서 QuickBuild 전체 흐름을 새로 설계할 필요는 없다.

**설정·사용성·OS**

- 사용자에게 긴 실행 명령이나 인증값을 매번 입력하게 하지 말고, 발견한 실제 실행 진입점과 기존 인증 참조를 재사용한다.
- 기존 profile의 명시적인 수집 설정은 유지한다. 새 연동은 선택 가능하게 추가하고 설치·인터페이스가 확인된 경우에만 자동 선택할 수 있게 한다. 설치되어 있다는 이유만으로 검증 완료 상태로 기록하지 않는다.
- 선택 우선순위는 이번 실행의 명시 지정 → 영구 사용자 설정 → 사용 가능성이 확인된 자동 선택으로 정리한다. 자동 선택·fallback 결과를 보고서에 표시한다.
- 자연어로 “사내 로그 수집 기능 사용”, “기존 FLA 수집 방식 사용”, “이 설정을 기본으로 저장”을 지정할 수 있게 한다. 실제 구현한 옵션만 사용 예에 기재한다.
- 기기별 실행 경로와 인증은 기존 `~/l1sw-local-environment/l1-fla/` 계열의 환경 설정 구조에 맞춘다. Windows 경로가 Linux 설정을 덮어쓰지 않게 한다.
- Python 자식 실행은 가능하면 현재 interpreter를 사용한다. Windows에 `python3`가 있다고 가정하지 말고 `python`·`py -3` 등 실제 사용 가능한 진입점을 확인한다.
- OS별 경로, 공백·한글 파일명, 인자 배열, subprocess timeout, 비대화형 실행을 확인한다.
- 기존 Analyzer 선택, 사용자 branch 설정, Jira 분석 포인트, MD/HTML 보고서와 Jira 링크 기능을 유지한다.

### 6. 검증

변경 전에 현재 테스트 실행법을 확인하고 기준 결과를 남긴다. v0.3.28에는 `tests/run_tests.py`가 있으며 현재 사용할 수 있는 Python으로 실행한다. 기존 실패와 이번 변경으로 생긴 실패를 구분한다.

새 테스트는 실제로 위험한 연결 경계에 집중한다. 통신을 흉내 낸 정상 응답만 확인하고 검증을 끝내지 않는다.

| 시나리오 | 확인 기준 |
|---|---|
| Jira 표현 차이 | 최신 댓글, 본문, 첨부, Remote Link, 포털 내부 파일을 실제 지원 범위에 맞게 찾고 선택 근거 보존 |
| 사내 기능의 대표 성공 사례 | 동일한 Jira 입력으로 기존 사내 수집과 FLA 연동 결과를 비교. 빠진 로그와 추가 선택 이유 설명 |
| 복수 이슈의 혼합 결과 | 예: 5건 중 정상 2건·로그 없음 1건·권한 오류 1건·정책 제외 1건. 다음 이슈까지 처리되고 전체 보고서에 모두 표시 |
| 부분 성공 | 여러 파일 중 일부 실패가 남고 유효한 파일의 처리가 정의된 정책대로 진행 |
| 재실행·재개 | 검증된 기존 파일을 반환하며 중복 다운로드·다른 이슈 파일 혼입 방지 |
| 거짓 성공 | 종료 코드 0 + 파일 없음, 로그인 HTML, 존재하지 않는 반환 경로를 성공 처리하지 않음 |
| 기존 사용자 | 기존 수집 방식과 profile, log-only 모드, 분석기 선택, 보고서 동작 보존 |
| OS 경계 | Windows/Linux 경로·interpreter 선택과 공백·한글 인자 처리 확인 |

실제 Jira를 사용자가 이미 지정했다면 해당 범위에서 대표 사례를 검증한다. 지정이 없으면 fixture로 구현·회귀 검증을 먼저 완료하고, 실환경 검증에 필요한 Jira만 마지막에 요청한다. 승인된 대상 없이 광범위한 Jira 검색이나 대량 다운로드를 시작하지 않는다.

한 OS에서만 실행했다면 다른 OS 검증을 완료했다고 하지 않는다. fixture 통과와 실제 서버에서의 성공을 구분한다. 다운로드 없이 계획만 확인하는 실행은 실제 파일 수집 검증을 대신하지 않는다.

### 7. 산출물과 완료 판정

기존 개인 스킬 구조를 유지하면서 필요한 수집 기능을 별도 모듈로 추가한다. 사내 공유 스킬 원본을 변경해야만 가능한 부분이 있다면 우선 FLA 쪽 연결·분리로 해결할 수 있는지 검토한다. 기존 사용자 변경과 영구 설정을 보존한다.

다음 결과를 사내 작업공간에 제공한다.

1. **수정된 L1-FLA**: 실제 호출 가능한 코드, CLI/설정, 자연어 안내, 필요한 회귀 테스트.
2. **통합 검토 기록**: 두 스킬 버전·경로, 확인한 수집 방식, 선택한 연결 방식과 근거, 의존성, 수정 파일.
3. **검증 기록**: 실행 명령, 성공/실패/미실행 항목, 실제 서버 검증 여부와 남은 제한.
4. **간단한 사용법**: 복수 Jira 실행, 수집 방식 선택·기본값 저장, 실패 후 재개, 기존 방식으로 전환.
5. 기존 배포 방식이 ZIP이면 버전과 날짜를 갱신한 패키지. 이름은 `l1-fla_v<버전>_<MMDD>.zip` 형식을 따르고 날짜 뒤에 설명을 붙이지 않는다. 임시 로그·자격증명·캐시를 패키지에 포함하지 않는다.

코드·문서·보고서의 산출 위치는 개인 스킬의 기존 artifact/output 관례를 따른다. 무관한 모뎀 코드 저장소 아래에 작업 보고서를 생성하지 않는다. 사내 원본이나 실제 로그를 외부 전송할 필요는 없다. 외부 GitHub 업로드는 이 작업 범위에 포함하지 않는다.

완료 보고는 다음 형식으로 짧게 작성한다.

- 채택 방식: 호출 / 이식 / 부분 결합, 선택 이유 한 문장.
- 실제 개선: 인식 가능한 로그 표현·다운로드 경로와 복수 이슈 처리 결과.
- 사용 방법: 실제 동작하는 자연어 예시와 명령.
- 검증 결과: 오프라인/실서버/OS별 결과를 구분.
- 남은 항목: 미확인 의존성이나 필요한 입력만 표시.

**수집기 등록, 문서 수정, 연결용 빈 함수 작성만으로 구현 완료라고 보고하지 않는다.** 원본 근거로 탐색부터 로컬 파일 전달까지 연결되고, 복수 이슈에서 실패가 기록되며 다음 이슈가 계속 처리되는지 확인해야 한다. 실환경 검증이 남았다면 “구현·오프라인 검증 완료, 실환경 검증 필요”로 정확히 보고한다.

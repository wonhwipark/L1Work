# format_profiles

P1이 실제 `_l1sw.txt` 샘플에서 역설계한 출력 형식 명세를 저장한다.

예:

```text
L1SW_OUTPUT_FORMAT_PROBED_20260627_1221_KST.md
```

이 폴더는 `kg_root/runtime_tool_generate/format_profiles/` 아래에 있어야 한다. `prompt/` 폴더는 legacy 수동 프롬프트용이므로 신규 runtime 산출물을 저장하지 않는다.

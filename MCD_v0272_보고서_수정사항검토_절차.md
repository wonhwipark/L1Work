# [사내 실행용] v0.2.72 — 보고서 생성부터 수정사항 검토까지

소요 약 20분 · STEP 4까지는 읽기 전용

> **경로는 `<...>` 로 표시했다. 실행 전에 실제 값으로 바꿀 것.**

---

## 0. 무엇이 고쳐졌나

`NO_EDGE_INPUT` / `work_units 0개` 의 원인이 확인되었다.
**읽는 순서가 아니라 relation 후보 선택 규칙이 문제였다.**

```
sam_metrics_mcd.csv                        폴더 요약. FromPath/ToPath 없음
sam_metrics_mcd_detail_mfs_relations.csv   순환 범위 부분집합
sam_metrics_mcd_detail_all_relations.csv   31,061줄 전체 의존
```

v0.2.71 까지 `choose("RELATION")` 의 tie-break 힌트가 `("mfs_relations",)`
하나뿐이라 **두 파일이 함께 있으면 항상 좁은 쪽을 집었다.**
그 결과 의존 집합이 SAM 이 이미 묶어둔 순환으로 잘리고 `work_units` 가 비었다.

**v0.2.72 는 관계 행 수로 고른다.** 넓은 export 가 이긴다.
행 수가 같으면 여전히 추측하지 않는다.

이제 `sam_metrics_mcd.csv` 를 지정해도 companion 탐색이 `all_relations` 를 찾아간다.
수동으로 지정할 필요가 없어졌다 — **STEP 2 에서 그것을 확인한다.**

---

## 채워 넣을 값

| 이름 | 설명 | 값 |
|---|---|---|
| `<ARTIFACT_DIR>` | SAM artifact CSV 들이 있는 **폴더** | |
| `<REL_CSV>` | `*all_relations*.csv` 절대경로 (교차검증용) | |
| `<BRANCH_ROOT>` | C++ 소스 루트 | `.../SMPF/Protocol/Channel/L1` |
| `<BUNDLE>` | 인계 번들 위치 (scripts/ 용) | |

경로를 모르면:

```
find ~ -name "sam_metrics_mcd*.csv" 2>/dev/null
```

---

## STEP 1 — 설치

```
python3 <BUNDLE>/install_all.py --only l1-sam-fixer
skillsilent run l1-sam-fixer version --
```

`0.2.72` 가 아니면 여기서 멈춘다. 이후가 무의미하다.

---

## STEP 2 — 정답 먼저 확보 ★ 순서를 지킬 것

도구를 거치지 않고 CSV 를 직접 파싱한다. **이 결과가 기준선이다.**

```
python3 <BUNDLE>/scripts/mcd_folder_cycles.py <REL_CSV>
```

기록:

```
rows =            files =            edges =
파일 단위 순환(SCC) =
leaf: 폴더 수 =        폴더 edge =        intra dropped =        순환 폴더 =
```

`ANS7|` 줄을 남긴다.

> 도구를 먼저 돌리고 그 결과를 정답으로 삼으면, 도구의 오류를 정답으로 착각한다.
> 반드시 이 순서다.

---

## STEP 3 — 보고서 생성

```
skillsilent run l1-sam-fixer mcd-report -- \
    --artifact-dir <ARTIFACT_DIR> \
    --view all --format all --json
```

`--artifact-dir` 로 **폴더**를 준다. 특정 CSV 를 지정하지 않는다.
v0.2.72 가 그 안에서 올바른 relation 파일을 고르는지 확인하는 것이 이번 핵심이다.

이어서:

```
skillsilent run l1-sam-fixer mcd-edge-leverage -- --top 20 --json
```

---

## STEP 4 — 결과 확인

```bash
python3 - <<'PY'
import json, glob, os
c = glob.glob(os.path.expanduser(
    "~/l1sw-private-skills/l1-sam-fixer/output/*/reports/mcd_edge_leverage.json"))
if not c:
    print("mcd_edge_leverage.json NOT FOUND"); raise SystemExit(1)
p = max(c, key=os.path.getmtime)
d = json.loads(open(p, encoding="utf-8").read())
g = d.get
print("file:", p)
for k in ("edge_source_health", "folder_granularity", "folder_count",
          "folder_edge_count", "intra_folder_edge_dropped", "folders_in_cycle",
          "folder_cyclic_scc_count", "self_loop_edge_dropped",
          "candidate_coverage_status"):
    print("  %-28s = %s" % (k, g(k)))
cand = d.get("folder_demotion_candidates") or []
print("  %-28s = %s" % ("demotion_candidates", len(cand)))
for x in cand[:5]:
    print("      %-34s freed=%s cut=%s refs=%s eff=%s" % (
        x.get("target_folder"), x.get("folders_freed"),
        x.get("edges_to_cut"), x.get("file_refs_to_change"),
        round(x.get("cost_efficiency") or 0, 3)))

run = os.path.dirname(os.path.dirname(p))
inv = os.path.join(run, "baseline", "inventory.json")
if os.path.isfile(inv):
    def find(o, k):
        if isinstance(o, dict):
            if k in o: return o[k]
            for v in o.values():
                r = find(v, k)
                if r is not None: return r
        elif isinstance(o, list):
            for v in o:
                r = find(v, k)
                if r is not None: return r
        return None
    raw = json.loads(open(inv, encoding="utf-8").read())
    b = find(raw, "mcd_score_bridge") or {}
    print("\n--- relation 선택 근거 (v0.2.72) ---")
    print("  status        =", b.get("status"), "/", b.get("reason_code"))
    print("  relation_file =", os.path.basename(str(b.get("relation_file"))))
    print("  rule          =", b.get("relation_selection_rule"))
    for k, v in (b.get("relation_row_counts") or {}).items():
        print("      %-52s rows=%s" % (os.path.basename(k), v))
    wu = find(raw, "work_units") or []
    edges = sum(len(u.get("dependency_edges") or []) for u in wu if isinstance(u, dict))
    print("  work_units =", len(wu), " dependency_edges =", edges)
    print("ANS15|health=%s|fold=%s|fe=%s|intra=%s|incyc=%s|ncand=%s|rel=%s|wu=%s|edges=%s" % (
        g("edge_source_health"), g("folder_count"), g("folder_edge_count"),
        g("intra_folder_edge_dropped"), g("folders_in_cycle"), len(cand),
        os.path.basename(str(b.get("relation_file"))), len(wu), edges))
PY
```

### 판정

| 관측 | 의미 | 다음 |
|---|---|---|
| `relation_file` 이 `*all_relations*` | **v0.2.72 수정이 동작했다** | 계속 |
| `relation_file` 이 `*mfs_relations*` | 넓은 파일을 못 봤다 | `relation_row_counts` 확인 |
| `wu` 가 49 근처, `health=OK` | 정상 | STEP 5 |
| `health=NO_EDGE_INPUT` | 아직 미해결 | `scanned_csvs` 확인 |
| `fold`/`incyc` 이 STEP 2 와 불일치 | 도구가 다른 데이터를 봄 | 차이 보고 |

**`ncand=0` 을 "고칠 것이 없다" 로 읽지 말 것.**
`health` 가 `OK` 가 아니면 "읽지 못했다" 는 뜻이다.

---

## STEP 5 — HTML 보고서에서 수정사항 확인

```
<run-dir>/reports/mcd_report.html
```

### 최상단 판정 배너를 먼저 읽는다

| 배너 | 의미 |
|---|---|
| **먼저 이것을 고치십시오** | 정상. 대상 폴더 · 해소 폴더 수 · 실제 코드 참조 제시 |
| **이 보고서는 아직 신뢰할 수 없습니다** | 수집이 깨짐. STEP 4 로 |
| **개선 후보가 만들어지지 않았습니다** | 순환 0 이면 정상 |

### 섹션 D — 폴더 단위 개선 후보

작업 목록이다. `고칠 코드` 대비 `해소 폴더` 순으로 정렬된다.

**`해소 폴더` 만 보지 말 것.** 같은 4개를 얻는 데 참조 1곳과 1218곳은 전혀 다른 일이다.

### 더 구체적인 지시서

```
python3 <BUNDLE>/scripts/mcd_demotion_plan.py <REL_CSV> --focus Export
python3 <BUNDLE>/scripts/mcd_demotion_plan.py <REL_CSV> --focus Utility
```

`[P-3]` 에 되참조 하나하나와 **그 뒤의 실제 파일 참조**가 나온다.

---

## STEP 6 — code-analyzer 연결 (선택)

`work_units` 가 정상이면 이제 edge 별 C++ 판정을 붙일 수 있다.

```
find ~/l1sw-private-skills/l1-sam-fixer/output -name code_analysis_request.json -newermt today

python3 ~/l1sw-private-skills/code-analyzer/scripts/mcd_edge_probe.py \
    --request-file <위 경로> \
    --branch-root <BRANCH_ROOT> \
    --output /tmp/edge_facts.json --json
```

```bash
python3 - <<'PY'
import json
from collections import Counter
d = json.loads(open("/tmp/edge_facts.json", encoding="utf-8").read())
facts = [e for w in (d.get("work_units") or [d]) for e in (w.get("edge_facts") or [])]
prop = Counter(e.get("edge_property") for e in facts)
cls = Counter(e.get("probe_class") for e in facts)
print("edges:", len(facts))
print("edge_property:", dict(prop))
print("probe_class  :", dict(cls))
print("ANS16|n=%s|fwd=%s|unknown=%s|unresolved=%s" % (
    len(facts), prop.get("FORWARD_DECLARABLE", 0), prop.get("UNKNOWN", 0),
    cls.get("SOURCE_FILE_NOT_RESOLVED", 0)))
PY
```

`unresolved` 가 대부분이면 `<BRANCH_ROOT>` 가 CSV 경로와 맞지 않는 것이다.
(이전 확인에서 `.../Protocol/Channel/L1` 로 92개 전부 직접 매칭됨)

그다음:

```
skillsilent run l1-sam-fixer improvement-points -- --top 5 --json
```

`evidence_level` 이 `ANALYSIS_REQUIRED` 에서 `CODE_ANALYZED` 로 올라가면
체인이 끝까지 뚫린 것이다.

---

## 회신 양식

```
ANS7|          (STEP 2, 정답 기준선)
ANS15|         (STEP 4, 도구 결과)
ANS16|         (STEP 6, probe 를 돌렸을 때)
```

### Q1. relation 선택

```
[ ] all_relations 선택됨 (v0.2.72 동작)
[ ] mfs_relations 선택됨
[ ] relation_file 없음
```

### Q2. STEP 2 와 STEP 4 의 폴더 수치

```
[ ] 일치     [ ] 불일치 -> STEP2 =        STEP4 = 
```

### Q3. HTML 배너

```
[ ] 먼저 이것을 고치십시오
[ ] 신뢰할 수 없습니다
[ ] 후보가 만들어지지 않았습니다
```

### Q4. 상위 demotion 후보 3건 (target / freed / cut / refs)

```

```

---

## 다음 — 보고서와 무관하게 병렬 진행

### `L1C/Common/Export` 되참조 1건

```
ch_L1cAllocatorUtil.hpp  ->  Utility/MRA/Common/ch_MraUtilRadio.cpp
```

참조 1개로 폴더 4개. 이미 확정된 값이라 보고서를 기다릴 필요가 없다.

**목적은 점수가 아니라 환산율이다.** 고정 base 에서 재야 한다.

```
M0   수정 전              -> S0
M0'  코드 변경 없이 재측정 -> S0r     <- 재현성 검사. 생략 금지
M1   수정 후              -> S1
```

`S1 - S0` 이 폴더 4개 해소의 실제 점수이며, 이 값이 나와야
나머지 폴더 작업의 가치와 3.77 도달 가능성을 계산할 수 있다.

### 재발 방지 게이트

개선 투입이 0건인 채 코드가 계속 들어와 지표가 악화 중이다.
`mcd_folder_cycles.py` 가 CSV 하나로 순환 폴더 수를 내므로
스킬 없이도 게이트를 만들 수 있다.

---

## 하지 말 것

| 항목 | 이유 |
|---|---|
| `mcd_folder_graph.py` 수정 | 정상이다. 입력이 문제였다 |
| `PROXY_MODEL_MISMATCH` 로직 삭제 | 정확한 진단이었다 |
| include graph 구축 | edge 는 이미 CSV 에 있다 |
| `HAL/.../SLEEP_BLOCK` 착수 | 참조 1218개로 폴더 4개 |
| 도구 완성을 기다리며 개선 보류 | 현재 악화의 직접 원인이다 |

# 그룹 Wiki MCP 조사 질문지 (객관식)

- 목적: wiki mcp의 **계약(입력·출력·소유 대상·판정 가능성)**을 확정해, 개인 `slte-knowledge-manager`의 방향(유지 / provider 소비 / 부분 위임 / 폐기)을 판단한다.
- 사용법: wiki mcp를 실제 호출해보고, 각 문항에서 **해당하는 번호 하나**(복수 허용 표기된 경우 여러 개)를 골라 결과만 회신.
- 판단 원칙: "누가 더 잘 아느냐"가 아니라 **소유 대상과 판정 가능성이 어디서 갈라지느냐**. 자연어 컨셉은 배경지식이고, 결정론 판정 근거는 별개 축.
- 회신 형식 예: `Q1: 2` / `Q3: 1,4` / `Q7: 근거—실제 응답 첫 줄 숫자코드만`

---

## A. 정체성 · 접근 (무엇이고 어떻게 부르나)

**Q1. wiki mcp의 실제 인터페이스 형태는?**
1. MCP 서버 (tool/resource 호출) — Claude Code에서 tool로 노출
2. REST 엔드포인트 (URL 직접 호출)
3. 로컬 파일/디렉토리 마운트 (경로 접근)
4. 기타/불명

**Q2. 호출 시 인증이 필요한가?**
1. 불필요 (그룹 내 자동)
2. PAT/토큰 필요
3. 별도 계정 로그인
4. 아직 확인 못 함

**Q3. wiki mcp가 노출하는 동작은? (복수 선택)**
1. 자연어 질의 → 관련 HLD 내용 반환 (semantic search)
2. 키워드/제목으로 문서 조회 (lookup)
3. 문서 목록/인벤토리 열거 (list)
4. 원문 전체 반환 (full document fetch)
5. 쓰기/등록 (지식 추가)
6. 기타/불명

---

## B. 내용 · 소유 대상 (무엇을 알고 있나) — **가장 중요**

**Q4. 반환되는 지식의 성격은? (복수 선택)**
1. 설계 컨셉/의도 (예: "Dual SIM은 이런 구조로 동작한다")
2. 모듈·블록별 책임 설명 (예: "TxSwitchMngr의 역할")
3. 브랜치별 차이/Variant (예: "1900 브랜치에서는 X가 Y로 바뀜")
4. 실패 모드·복구 조건 (예: "mismatch 시 URTG recovery 발생")
5. 값 불일치 판정용 correlation key (예: stack_id/cc_id 대조 규칙)
6. 코드 라인·심볼 레벨 근거 (file:line, 함수명)

**Q5. wiki가 학습한 HLD의 세분성은?**
1. 도메인/기능 컨셉 레벨 (사람이 읽는 설명)
2. 모듈·블록 단위 책임 레벨
3. 함수·클래스·상태 단위 레벨
4. 1~2번 위주, 3번은 거의 없음

**Q6. wiki에 "L1 Tx 경로(txpathmap, tx_onoff, tx_swap_rq 등)" 세부가 들어있나?**
1. 상세히 있음 (경로·순서·조건까지)
2. 개념적으로만 언급 (이름은 나오나 동작 세부는 없음)
3. 거의 없음
4. 확인 못 함

**Q7. wiki에 "브랜치 간 포팅 시 무엇이 어떻게 바뀌는가(Replacement/Variant/예외)"가 들어있나?** ← 개인 매니저 핵심 소유 대상
1. 있음 — 브랜치별 차이를 명시
2. 단일 기준 설계만 있고 브랜치 델타는 없음
3. 확인 못 함

---

## C. 출처 · 신선도 · SSOT (기준으로 삼을 수 있나)

**Q8. wiki가 학습한 Confluence HLD는 어느 계보인가?**
1. 사람이 작성한 공식 설계 HLD (Confluence 원본)
2. 코드에서 역생성된 baseline HLD (composer류 산출물)
3. 둘 다 섞임
4. 확인 못 함

**Q9. wiki 내용의 갱신 방식은?**
1. Confluence 변경 시 자동/주기적 재학습
2. 수동 재학습 (누군가 갱신해야 반영)
3. 1회성 스냅샷 (고정)
4. 확인 못 함

**Q10. 반환 결과에 출처 추적(문서 링크/버전/timestamp)이 붙나?**
1. 붙음 (어느 Confluence 문서·버전인지 확인 가능)
2. 내용만 반환, 출처 불명
3. 확인 못 함

---

## D. 판정 가능성 (근거로 쓸 수 있나) — 개인 매니저와의 경계선

**Q11. 같은 질의를 반복하면 동일 답이 나오나(결정론)?**
1. 동일 (결정론적)
2. 표현이 매번 조금씩 달라짐 (생성형)
3. 확인 못 함

**Q12. wiki 답변을 "값 불일치 확정 근거"로 쓸 수 있을 만큼 구체적인가?**
   (예: "이 CL은 tx_swap_rq를 안 거쳐서 mismatch다"를 단정할 수준)
1. 가능 — correlation key/조건까지 명시
2. 불가능 — 방향성 배경지식 수준 (단정 불가, open item 수준)
3. 확인 못 함

**Q13. wiki가 틀리거나 오래된 내용을 반환할 때, 이를 검증/승인하는 게이트가 있나?**
1. 있음 (그룹 차원 승인/검수 절차)
2. 없음 (학습된 대로 그냥 반환)
3. 확인 못 함

---

## E. 운영 · 거버넌스

**Q14. wiki에 개인이 지식을 추가/수정할 수 있나?**
1. 가능 (쓰기 권한 있음)
2. 읽기 전용 (소비만)
3. 확인 못 함

**Q15. wiki 응답 속도/분량은 사내 저사양 모델(Qwen3.6-27B) 파이프라인에서 실용적인가?**
1. 빠르고 간결 (agent 루프에 넣기 적합)
2. 느리거나 장문 (compact 유발 우려)
3. 확인 못 함

---

## F. 자유 관측 (선택)

**Q16.** 실제로 "Dual SIM" 또는 "TxSwitchMngr" 하나 질의해보고, 반환 첫 문단의 **성격**을 한 줄로:
- (예: "블록 책임을 서술형으로 3문장, 브랜치 언급 없음, 출처 링크 있음")

---

## 판단 매핑 (회신 후 내가 적용할 규칙 — 참고용)

| 관측 | 개인 매니저 방향 |
|---|---|
| Q7=2 **and** Q12=2 | **유지 확정** — wiki는 컨셉, 개인은 브랜치델타+결정론판정. provider로만 연결 |
| Q8=1 **and** Q10=1 | HLD 원문 소유를 wiki로 위임 → 개인 §12 `CONFLUENCE_HLD`를 wiki 참조로 대체 |
| Q4에 3·4·5 포함 **and** Q12=1 | **부분 겹침** — 겹치는 지식유형만 소비 전환 재검토 (단 승인 게이트는 개인 유지) |
| Q9=1 **and** Q13=1 | wiki 신선도·검수 신뢰 가능 → 개인 부트스트랩 소스로 적극 소비 |
| Q13=2 (검수 없음) | 개인 승인 게이트 **반드시 유지** — wiki 출력을 자동 승인 금지 |
| Q6=2/3 (Tx 세부 없음) | L1 Tx 특화 깊이는 개인 고유 → 폐기 대상 아님 확정 |

# SAM Result HTML 확인 가이드

> **목적**: `sam-result.html`(MCD)에서 순환별 스코어 패널티를 sam-fixer가 읽게 하려면
> **Parser Profile**(테이블 열 매핑)이 필요합니다. 이 가이드의 5개 항목을 확인해 답하면,
> 그 답으로 Parser Profile을 만들거나 대안을 설계할 수 있습니다.
>
> **중요 전제**: sam-fixer의 HTML 파서는 **`<table>` 태그만** 읽습니다.
> `<div>`로 그린 표, SVG/canvas 그래프, `<script>` 안 JSON 데이터는 읽지 못합니다.
> 그래서 **확인 4(스코어가 진짜 `<table>` 안에 있는가)가 가장 중요**합니다.

---

## 준비 — 두 가지 보기 방법

| 방법 | 여는 법 | 용도 |
|------|---------|------|
| 화면 보기 | 브라우저에서 그냥 열기 | 무엇이 보이는지 눈으로 확인 |
| 소스 보기 | 우클릭 → "페이지 소스 보기" (Ctrl+U), 또는 F12 → Elements | 실제 태그 구조 확인 |

일부 항목은 화면만으로, 일부는 소스를 봐야 정확합니다. 각 항목에 `[화면]` / `[소스]` 표시.

---

## 확인 1 — 스코어 패널티가 "표" 안에 있는가 `[화면]`

앞서 본 스코어 패널티(예: -4.01, -3.7)와 MCD 스코어(예: 3.5/5)가 화면에서 어떻게 보이는지.

**답할 것:**
- [ ] 이 숫자들이 **행·열이 있는 표(테이블)** 안에 있나요?
      아니면 그래프 그림 위/옆의 텍스트인가요? 카드/패널 형태인가요?
- [ ] (표 안이면) 그 표의 **열 제목(헤더)**을 보이는 대로 전부 적어주세요.
      예: `Module | Cycle Path | Score Penalty | Max Cycle Count`

```
[여기에 답 작성]


```

> 가장 중요한 항목. 표 안이면 sam-fixer가 바로 읽습니다.
> 그래프 위 텍스트/카드면 다른 방법이 필요합니다.

---

## 확인 2 — 순환 목록이 표로 있는가 `[화면]`

스코어와 별개로, **순환들의 목록**(어느 모듈들이 순환하는지)이 표로 있는지.

**답할 것:**
- [ ] 순환 목록이 표로 있나요? 있다면 그 표의 **열 제목**을 그대로 적어주세요.
- [ ] 그 표의 **한 행(row) 예시**를 하나 적어주세요.
      예: `TxCfgMngr → TxSwitchMngr | -4.01 | 3` 처럼 실제 값.
- [ ] 이 순환 목록 표가 **확인 1의 스코어 표와 같은 표**인가요, 다른 표인가요?

```
[여기에 답 작성]


```

---

## 확인 3 — 표가 몇 개이고 각각 뭘 담나 `[화면]`

HTML 전체에 표가 여러 개일 수 있습니다.

**답할 것:**
- [ ] 페이지에 표가 총 몇 개쯤 보이나요?
- [ ] 각 표가 대략 뭘 담고 있나요?
      예: "1번째 표 = 전체 요약 점수 / 2번째 표 = 순환 목록 / 3번째 표 = 파일별 상세"

```
[여기에 답 작성]


```

> sam-fixer는 표들을 순서대로 읽으므로, 몇 번째 표에 필요한 게 있는지 알아야 합니다.

---

## 확인 4 — 소스에서 `<table>` 태그 확인 `[소스]` ★ 가장 중요

화면엔 표처럼 보여도 실제로는 `<div>`로 만든 가짜 표일 수 있습니다.
sam-fixer는 진짜 `<table>` 태그만 읽습니다.

**확인 방법:**
1. Ctrl+U(소스 보기) 또는 F12 → Elements
2. Ctrl+F로 페이지 내 검색

**답할 것:**
- [ ] **`<table`** 을 검색하면 나오나요? 몇 개나 나오나요?
- [ ] 스코어 패널티 숫자(예: `-4.01`)를 소스에서 Ctrl+F로 검색하면,
      그 숫자가 아래 어디에 있나요? (해당하는 것에 표시)
  - [ ] `<td>` 또는 `<th>` 태그 안  → **sam-fixer가 바로 읽음 (최선)**
  - [ ] `<div>` 또는 `<span>` 안     → 테이블 파서로 못 읽음, 대안 필요
  - [ ] `<script>` 안의 데이터(JSON 등) → 그래프 데이터, 별도 추출 필요
  - [ ] 기타 (무엇인지 적기)

```
[여기에 답 작성]
<table 검색 결과 개수:
-4.01 이 들어있는 태그:


```

> 결정적 항목. 스코어가 `<td>` 안이면 바로 되고, 아니면 추가 작업이 필요합니다.

---

## 확인 5 — 스코어와 순환이 연결되는가 `[화면 또는 소스]`

목표는 "각 순환에 스코어를 붙여 정렬"입니다.
그러려면 어느 스코어가 어느 순환 것인지 연결돼야 합니다.

**답할 것:**
- [ ] 각 스코어 패널티(예: -4.01)가 **어느 순환(어느 모듈 쌍)의 것인지**
      명확히 연결돼 보이나요? (같은 행/같은 위치에 있나요?)
- [ ] 아니면 스코어는 전체 총점만 있고, 개별 순환별 스코어는 없나요?

```
[여기에 답 작성]


```

> 개별 순환별 스코어가 있어야 "상위 몇 개"를 정할 수 있습니다.
> 총점만 있으면 우선순위를 다른 방식으로 정해야 합니다.

---

## 답변 후 진행 (참고)

확인 결과에 따라 세 경우로 갈립니다.

| 경우 | 조건 | 다음 진행 |
|------|------|-----------|
| 1 (최선) | 스코어·순환이 진짜 `<table>` 안, 순환별로 연결됨 | 열 제목으로 **Parser Profile** 작성 → import/activate → sam-fixer가 스코어순 순환 목록 산출 |
| 2 | 스코어가 `<div>`/`<script>` JSON에 있음 (테이블 아님) | 테이블 파서로 못 읽음 → HTML에서 JSON/텍스트 추출하는 **별도 스크립트** 또는 스코어 수동 입력 설계 |
| 3 | 개별 순환별 스코어 없이 총점만 | HTML로 우선순위 불가 → CSV의 다른 신호(순환 크기·폴더 수 등)로 **대체 우선순위** 설계 |

**우선 확인 4부터.** 스코어가 `<table>`/`<td>` 안이냐 아니냐가 전체 방향을 가릅니다.

---

## 확인된 사실 요약 (지금까지)

이 가이드는 아래 확정 사실 위에서 작성됨:

- **CSV 구조**: 163 edge / 고유 cycle_id 34개 / 실제 순환 52개
  (cycle_id 하나에 여러 경로가 섞임 — 예: id 58 = 3개 경로)
- **순환 폴더 분포**: 2폴더 36개, 3폴더 11개, 4폴더 3개, 5폴더 2개
  (70%는 2폴더, 30%는 3폴더 이상 → 세 폴더 이상은 fix 난이도 다름)
- **CSV 열**: cycle_id, from(파일), to(파일), 함수명, 클래스명, from line, Violation(Y/N), relationcount(전부 1)
- **CSV에 없는 것**: 순환별 우선순위 → **HTML의 스코어 패널티가 이 역할** ← 이 가이드가 확인하려는 대상
- **병합 버그**: sam-fixer가 163 edge를 그대로 163 Work Unit으로 만듦 (52 순환으로 묶여야 함)
- **HTML 파서 제약**: `<table>` 태그만 읽음. Parser Profile(열 매핑) 등록 필요

**목표**: 스코어 패널티 상위 N개 순환을 골라 먼저 고쳐 MCD 스코어(3.5/5)를 올린다.

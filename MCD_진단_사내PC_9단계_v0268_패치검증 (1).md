# [사내PC 실행용 · 9단계] v0.2.68 폴더 단위 패치 검증

소요 약 10분 · 스킬 설치 필요 (v0.2.68) · 코드 수정 없음

> 회신은 **객관식 5문항 + 표 한 개**.

---

## 0. 목적

v0.2.68에 `mcd_folder_graph.py` 와 `demotion_candidates()` 가 들어갔다.
확인할 것은 두 가지다.

1. **계산이 맞는가** — 실제 CSV에서 앞서 수동 분석한 값이 재현되는가
2. **배포물에 실리는가** — `mcd-report` 에 demotion 제안이 나오는가

코드를 읽어본 결과 2번은 **안 나올 가능성이 높다.**
`mcd_report.py` 와 `mcd_improvement_points.py` 에 `folder`/`demotion` 참조가 **0회**다.
`mcd-report` 가 `mcd_edge_leverage.json` 을 읽기는 하지만 `all_edges`/`edges` 키만 꺼낸다.
이 단계는 그 추정을 실측으로 확정한다.

---

## 1. 사전 확인 — 알려진 결함 1건

`--folder-depth` 인자가 `l1_sam_fixer.py` 에는 있지만
`skillsilent/policy.json` 의 `allowed_flags` 에는 **빠져 있다.**

```
skillsilent run ... -- --folder-depth d2   -> 정책 게이트에서 거부됨
```

기본값 `leaf` 로는 정상 동작하므로 아래 절차에는 지장이 없다.
**granularity를 바꿔 볼 수는 없다는 점만 알고 진행한다.**

---

## 2. STEP 1 — 스킬 업데이트

```
(기존 절차대로 l1-sam-fixer v0.2.68 설치)
skillsilent run l1-sam-fixer version --
```

`0.2.68` 확인.

---

## 3. STEP 2 — artifact 적재

```
skillsilent run l1-sam-fixer import-artifact -- --artifact-dir <SAM artifact 폴더> --metric MCD --json
```

**7~8단계에서 쓴 것과 같은 artifact**를 쓴다. 값 비교가 목적이다.

---

## 4. STEP 3 — edge leverage 실행

```
skillsilent run l1-sam-fixer mcd-edge-leverage -- --top 20 --json > lev.json
```

생성물도 함께 확인:

```
<run-dir>\reports\mcd_edge_leverage.json
<run-dir>\reports\mcd_edge_leverage.md
```

`.md` 에서 **`Upper-boundary Demotion Candidates`** 섹션을 찾는다.

---

## 5. STEP 4 — 보고서 실행

```
skillsilent run l1-sam-fixer mcd-report -- --view all --format all --json
```

생성된 `mcd_report.md` / `.html` 에서 **`demotion`** 또는 **`Folders freed`** 를 검색한다.

```
findstr /i "demotion" <run-dir>\reports\mcd_report.md
findstr /i "demotion" <run-dir>\reports\mcd_improvement_points.md
```

---

## 6. 회신

### Q1. `mcd-edge-leverage` 에 demotion 섹션이 나오는가

```
[ ] 나온다
[ ] 안 나온다  -> payload의 candidate_coverage_status = 
```

### Q2. demotion 후보 표 — 상위 5행 그대로 붙여넣기

```
| # | Target | Folders freed | Edges to cut | File refs | Efficiency | Dependents |



```

### Q3. 수동 분석값이 재현되는가

7~8단계 수동 분석 결과와 대조:

| 기대 | 재현? |
|---|---|
| `L1C/Common/Export` → freed 4 / cut 1 / refs 1 | `[ ] 일치  [ ] 불일치  [ ] 목록에 없음` |
| `Utility/MRA/Common` → freed 6 / cut 3 / refs 9 | `[ ] 일치  [ ] 불일치  [ ] 목록에 없음` |
| `folders_in_cycle` = 46 | `[ ] 일치  [ ] 불일치 -> 값 = ` |

### Q4. `mcd_report.md` 에 demotion 이 포함되는가

```
[ ] 포함됨
[ ] 없음        <- 예상되는 결과
```

### Q5. `mcd_improvement_points.md` 상태

```
[ ] demotion 후보가 개선 포인트로 올라옴
[ ] 여전히 ANALYSIS_REQUIRED 만 있음     <- 예상되는 결과
[ ] 목록이 아예 비어 있음
```

---

## 7. 결과별 다음 단계 (참고용)

| 관측 | 결론 | 다음 |
|---|---|---|
| Q1 O, Q3 일치, Q4 없음 | **계산부 완성, 배선만 남음** | `mcd_report.py` 에 `folder_demotion_candidates` 소비 경로 추가. 렌더 코드는 `mcd_edge_leverage.py` 에 이미 있으니 재사용 |
| Q1 O, Q3 불일치 | 계산 로직 차이 | granularity/정규화 확인. 수동 스크립트와 대조 |
| Q1 X | demotion이 생성 자체가 안 됨 | `candidate_coverage_status` 확인. 폴더 그래프 진입 조건 점검 |
| Q5 = ANALYSIS_REQUIRED | 개선 포인트가 여전히 파일 단위 | 파일 단위 후보 0일 때 demotion을 승격하도록 수정 |

---

## 8. 주의

- **담당자에게 배포하는 것은 `mcd-report` 다.** 거기에 안 실리면 계산이 맞아도 실무에 전달되지 않는다. Q4가 이 단계의 핵심 질문이다.
- Q3의 수치가 불일치해도 곧바로 결함은 아니다. 수동 스크립트는 CSV를 직접 읽고, 스킬은 `inventory.json` 을 거친다. 차이가 나면 **어느 쪽이 맞는지**를 따로 확인해야 한다.
- 이 단계는 코드를 수정하지 않는다. 실행과 확인만 한다.
